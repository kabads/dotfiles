- defaults:
    link:
      relink: true

# - clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.vimrc:
    ~/.vim:
      create: true
      glob: true
    ~/.fzf.zsh:
    ~/.emacs:

- create:
    - ~/.vim/swap
    - ~/.vim/bundle
  #   - ~/downloads
  #   - ~/.vim/undo-history
  # - ~/Work
  # - ~/hey/you/guys
# - shell:
#   - [git submodule update --init --recursive, Installing submodules]

