- defaults:
    link:
      relink: true

# - clean: ['~']

      #- create:
      # - ~/.vim/swap
- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.vimrc:
    ~/.vim:
      # ~/.vim/bundle/vim-fugitive:  
    ~/.fzf.zsh:
    ~/.emacs:
    ~/.oh-my-zsh:
    ~/.bashrc:
    ~/.bash_profile:
    ~/.python-version:
    ~/.xinitrc:
    ~/.key_bindings.zsh
  #   - ~/downloads
  #   - ~/.vim/undo-history
  # - ~/Work
  # - ~/hey/you/guys
- shell:
  - [git submodule update --init --recursive, Installing submodules]

