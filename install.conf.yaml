- defaults:
    link:
      relink: true

# - clean: ['~']

- link:
  # ~/.vim: vim
  # ~/.vimrc: vimrc
    ~/.zshrc:
    ~/.gitconfig:
    ~/.vimrc:
    ~/.vim:
      glob: True
    ~/.fzf.zsh:
    ~/.emacs:

- create:
    - ~/.vim/swap
    - ~/.vim/bundle
      #   - ~/downloads
  #   - ~/.vim/undo-history
  # - ~/Work
  # - ~/hey/you/guys
# - shell:
#   - [git submodule update --init --recursive, Installing submodules]

