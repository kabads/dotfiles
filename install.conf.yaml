- defaults:
    link:
      relink: true

- clean: ['~']

- link:
  # ~/.vim: vim
  # ~/.vimrc: vimrc
    ~/.zshrc:
    ~/.gitconfig:
    ~/.vimrc:
# - create:
  #   - ~/downloads
  #   - ~/.vim/undo-history
  # - ~/Work
  # - ~/hey/you/guys
# - shell:
#   - [git submodule update --init --recursive, Installing submodules]

